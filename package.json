{
  "name": "openpage",
  "version": "0.1.0",
  "private": true,
  "description": "OpenPage — a free, global digital library",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist",
    "deploy:vercel": "vite build && npx vercel --prod"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vercel": "^54.9.1",
    "vite": "^6.4.2"
  }
}
